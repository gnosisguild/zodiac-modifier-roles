# Devcontainer Claude Notes

- This is a Solidity/Hardhat project for the Zodiac Roles Modifier.
- Run tests with `yarn test`.
- Run coverage with `yarn coverage`.
- Compile contracts with `yarn build`.
