"use client"

import { Permission } from "../types"

const AnnotatedPermissionGroup: React.FC<{
  permissions: Permission[]
  values: Record<string, string>
  schema: 
}> = ({ permissions, values, schema }) => {
  return null
}

export default AnnotatedPermissionGroup
