import type { MetaRecord } from "nextra";

const meta: MetaRecord = {
  "lower-threshold-routine-transactions": {
    title: "Lower Safe Threshold for Routine Transactions",
  },
};

export default meta;
