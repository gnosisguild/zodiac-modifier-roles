{
  "name": "zodiac-roles-subgraph",
  "license": "LGPL-3.0+",
  "scripts": {
    "codegen": "yarn prepare:mainnet && graph codegen",
    "build": "graph build",
    "format": "prettier '(test|src)/**/*.ts' -w",
    "deploy:local": "graph deploy --node http://localhost:8020/ --ipfs http://localhost:5001 gnosis/zodiac-roles-mod",
    "deploy:mainnet": "yarn prepare:mainnet && bash -c 'source .env || true && graph deploy --studio $SUBGRAPH_MAINNET'",
    "deploy:goerli": "yarn prepare:goerli && bash -c 'source .env || true && graph deploy --studio $SUBGRAPH_GOERLI'",
    "deploy:gnosis-chain": "yarn prepare:gnosis-chain && bash -c 'source .env || true && graph deploy --studio $SUBGRAPH_GNOSIS_CHAIN'",
    "deploy:arbitrum-one": "yarn prepare:arbitrum-one && bash -c 'source .env || true && graph deploy --studio $SUBGRAPH_ARBITRUM_ONE'",
    "deploy:avalanche": "yarn prepare:avalanche && bash -c 'source .env || true && graph deploy --node https://api.thegraph.com/deploy/ $SUBGRAPH_AVALANCHE'",
    "deploy:bsc": "yarn prepare:bsc && bash -c 'source .env || true && graph deploy --node https://api.thegraph.com/deploy/ $SUBGRAPH_BSC'",
    "deploy:optimism": "yarn prepare:optimism && bash -c 'source .env || true && graph deploy --node https://api.thegraph.com/deploy/ $SUBGRAPH_OPTIMISM'",
    "deploy:polygon": "yarn prepare:polygon && bash -c 'source .env || true && graph deploy --node https://api.thegraph.com/deploy/ $SUBGRAPH_POLYGON'",
    "deploy:optimism-on-gnosis-chain": "yarn prepare:optimism-on-gnosis-chain && bash -c 'source .env || true && graph deploy --node https://api.thegraph.com/deploy/ $SUBGRAPH_OPTIMISM_ON_GNOSIS_CHAIN'",
    "prepare:mainnet": "mustache network_configs/mainnet.json subgraph.template.yaml > subgraph.yaml",
    "prepare:goerli": "mustache network_configs/goerli.json subgraph.template.yaml > subgraph.yaml",
    "prepare:gnosis-chain": "mustache network_configs/gnosis-chain.json subgraph.template.yaml > subgraph.yaml",
    "prepare:arbitrum-one": "mustache network_configs/arbitrum-one.json subgraph.template.yaml > subgraph.yaml",
    "prepare:avalanche": "mustache network_configs/avalanche.json subgraph.template.yaml > subgraph.yaml",
    "prepare:bsc": "mustache network_configs/bsc.json subgraph.template.yaml > subgraph.yaml",
    "prepare:optimism": "mustache network_configs/optimism.json subgraph.template.yaml > subgraph.yaml",
    "prepare:polygon": "mustache network_configs/polygon.json subgraph.template.yaml > subgraph.yaml",
    "prepare:optimism-on-gnosis-chain": "mustache network_configs/optimism-on-gnosis-chain.json subgraph.template.yaml > subgraph.yaml"
  },
  "devDependencies": {
    "mustache": "^4.2.0",
    "prettier": "^2.5.1"
  },
  "dependencies": {
    "@graphprotocol/graph-cli": "^0.44.0",
    "@graphprotocol/graph-ts": "^0.29.3"
  }
}
